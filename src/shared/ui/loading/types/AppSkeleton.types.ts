export interface AppSkeletonProps {
  variant?: "list" | "dashboard";
  rows?: number;
  showHeader?: boolean;
  showAvatar?: boolean;
  fullPage?: boolean;
  showTopCards?: boolean;
  tableRows?: number;
}