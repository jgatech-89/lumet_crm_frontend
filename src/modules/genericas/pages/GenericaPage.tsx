import Typography from "@mui/material/Typography";

import { Layout } from "@/shared/layout";

export function GenericaPage() {
  return (
    <Layout>
      <Typography variant="h4">Genericas</Typography>
    </Layout>
  );
}
